# 🔧 修复 Netlify CLI 安装问题

## 问题原因

你使用的是 Node.js v25.2.1，而 `netlify-cli@17.0.0` 与这个版本不兼容。

## 解决方案

### 方案 1: 使用 Vite Dev Server（推荐，快速测试前端）

如果你只想测试前端 UI 和基本功能，不需要 OCR API：

```bash
npm run dev
```

然后访问: http://localhost:3000

**优点**:
- ✅ 不需要 Netlify CLI
- ✅ 启动快速
- ✅ 适合测试 UI 和交互

**缺点**:
- ❌ 无法测试 OCR 功能（因为 Functions 不运行）

### 方案 2: 升级 Netlify CLI 版本

我已经更新了 `package.json`，使用更新的版本：

```bash
# 删除旧的 node_modules
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

如果还是有问题，尝试：

```bash
# 使用 npx 直接运行（不安装到项目）
npx netlify-cli@latest dev
```

### 方案 3: 使用 Node.js 版本管理器（推荐用于生产）

如果你需要完整功能，建议使用 Node.js 18 或 20：

```bash
# 使用 nvm (如果已安装)
nvm install 20
nvm use 20

# 然后重新安装依赖
npm install
npm run dev:netlify
```

### 方案 4: 临时解决方案 - 模拟 Functions

创建一个简单的本地代理来模拟 Functions：

```bash
# 创建简单的代理服务器
node test-server.js
```

## 🚀 快速开始（使用 Vite）

1. **启动前端服务器**
   ```bash
   npm run dev
   ```

2. **打开浏览器**
   - 访问: http://localhost:3000

3. **启用移动模式**
   - 按 `F12` → 点击设备图标
   - 选择移动设备

4. **测试功能**
   - ✅ 测试 UI 和交互
   - ✅ 测试图片上传
   - ✅ 测试界面布局
   - ❌ OCR 功能需要 Netlify Dev

## 📝 当前状态

- ✅ 前端可以正常运行 (`npm run dev`)
- ❌ Netlify Functions 需要 Netlify CLI
- ✅ 可以使用 Vite Dev Server 测试大部分功能

## 💡 建议

对于开发和测试：
1. **UI 测试**: 使用 `npm run dev` (端口 3000)
2. **完整测试**: 解决 Netlify CLI 问题后使用 `npm run dev:netlify` (端口 8888)
3. **生产部署**: 直接部署到 Netlify，不需要本地 Netlify CLI

