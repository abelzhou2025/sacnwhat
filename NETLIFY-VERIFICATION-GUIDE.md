# 📋 Netlify 验证部署详细指南

## 🔍 步骤 1: 查看构建日志

### 路径导航

1. **登录 Netlify**
   - 访问: https://app.netlify.com/
   - 使用你的账号登录

2. **进入你的站点**
   - 在 Dashboard 中找到你的站点（sacnwhat 或 scanwhat）
   - 点击站点名称进入详情页

3. **查看部署列表**
   - 点击顶部菜单的 **"Deploys"** 标签
   - 或直接访问: `https://app.netlify.com/sites/[你的站点名]/deploys`

4. **打开构建日志**
   - 找到最新的部署（通常在最上面）
   - 点击部署条目（会展开详情）
   - 或点击部署右侧的 **"View build log"** 按钮
   - 构建日志会显示在页面下方

### 在构建日志中检查什么

✅ **构建成功标志**:
```
✓ Built in Xs
dist/index.html
dist/_redirects
dist/assets/...
```

❌ **如果看到错误**:
- 查看红色错误信息
- 检查 Node.js 版本
- 检查依赖安装是否成功

---

## 📁 步骤 2: 检查部署的文件

### 方法 A: 在部署详情中查看

1. **在 Deploys 页面**
   - 点击最新的部署条目
   - 部署详情会展开

2. **查看文件列表**
   - 在部署详情中，找到 **"Browse files"** 或 **"View files"** 按钮
   - 点击后会显示所有部署的文件

3. **检查关键文件**
   - 查找 `index.html` ✅
   - 查找 `_redirects` ✅（应该在根目录）
   - 查找 `assets/` 目录 ✅

### 方法 B: 使用 Netlify 文件浏览器

1. **在站点详情页**
   - 点击 **"Deploys"** 标签
   - 找到最新的部署
   - 点击部署右侧的 **"..."** 菜单
   - 选择 **"Browse files"** 或 **"View deploy"**

2. **文件结构应该类似**:
```
dist/
├── index.html          ← 必须存在
├── _redirects          ← 必须存在（这是关键！）
├── assets/
│   ├── index-[hash].js
│   └── index-[hash].css
└── ...
```

---

## 🔎 步骤 3: 确认 _redirects 文件存在

### 检查方法

1. **在文件浏览器中**
   - 滚动到根目录
   - 查找 `_redirects` 文件
   - 如果找不到，说明构建有问题

2. **查看文件内容**（如果能看到）
   - 点击 `_redirects` 文件
   - 应该看到类似内容：
   ```
   /.netlify/functions/*  /.netlify/functions/:splat  200
   /*  /index.html  200
   ```

### 如果 _redirects 文件不存在

**可能的原因**:
1. Vite 没有复制 `public/` 目录
2. 构建配置有问题
3. 文件路径不正确

**解决方案**:
- 检查构建日志中是否有关于 `public` 目录的错误
- 确认 `public/_redirects` 文件在 GitHub 仓库中
- 重新触发部署

---

## 🛠️ 步骤 4: 检查 Netlify 设置

### 路径导航

1. **进入站点设置**
   - 在站点详情页
   - 点击 **"Site settings"**（通常在右上角或左侧菜单）

2. **检查构建设置**
   - 点击左侧菜单的 **"Build & deploy"**
   - 或直接访问: `https://app.netlify.com/sites/[你的站点名]/configuration/deploys`

3. **验证以下设置**:

   **Build settings**:
   ```
   Base directory: (留空)
   Build command: npm ci && npm run build
   Publish directory: dist
   ```

   **Environment variables**:
   - 检查是否设置了 `NODE_VERSION = 18`（或在构建环境中）
   - 如果需要 OCR 功能，检查 `DEEPSEEK_API_KEY` 或 `GEMINI_API_KEY`

   **Functions**:
   ```
   Functions directory: netlify/functions
   ```

---

## 🧪 步骤 5: 测试网站

### 测试方法

1. **访问网站**
   - 在站点详情页，找到你的网站 URL
   - 通常显示在顶部，格式: `https://[站点名].netlify.app`
   - 点击访问

2. **测试路由**
   - 访问首页 ✅
   - 刷新页面 ✅（不应该 404）
   - 直接访问 `/test` 路径 ✅（应该显示应用，不是 404）

3. **检查浏览器 Console**
   - 按 `F12` 打开开发者工具
   - 查看 **Console** 标签 - 不应该有错误
   - 查看 **Network** 标签 - 所有请求应该是 200

---

## 📊 快速检查清单

在 Netlify Dashboard 中验证：

- [ ] **Deploys 页面**: 最新部署显示 "Published" ✅
- [ ] **构建日志**: 显示 "Build successful" ✅
- [ ] **文件列表**: 包含 `_redirects` 文件 ✅
- [ ] **构建设置**: Build command 和 Publish directory 正确 ✅
- [ ] **网站访问**: 可以正常打开，不显示 404 ✅

---

## 🐛 如果仍然 404

### 调试步骤

1. **检查构建日志**
   - 是否有错误信息？
   - `_redirects` 文件是否被复制？

2. **检查文件结构**
   - `_redirects` 是否在 `dist` 目录的根目录？
   - 文件内容是否正确？

3. **检查重定向规则**
   - 在 Netlify Dashboard → Site settings → Redirects
   - 查看是否有重定向规则
   - 应该看到来自 `netlify.toml` 的规则

4. **清除缓存重新部署**
   - Deploys → Trigger deploy → **Clear cache and deploy site**

---

## 📝 截图位置参考

### 在 Netlify Dashboard 中的位置

```
Netlify Dashboard
└── [你的站点名]
    ├── Overview (概览)
    ├── Deploys (部署) ← 在这里查看构建日志和文件
    │   └── [最新部署]
    │       ├── View build log (查看构建日志)
    │       └── Browse files (浏览文件)
    ├── Site settings (站点设置)
    │   ├── Build & deploy (构建和部署)
    │   │   ├── Build settings (构建设置)
    │   │   └── Environment variables (环境变量)
    │   └── Functions (函数)
    └── ...
```

---

## 💡 提示

- 如果找不到某个选项，尝试使用 Netlify 的搜索功能
- 所有设置都在 **Site settings** 中
- 部署信息都在 **Deploys** 标签中
- 如果还是找不到，可以查看 Netlify 的文档或联系支持

---

## 🔗 直接链接格式

如果你的站点名是 `your-site-name`，可以直接访问：

- 部署列表: `https://app.netlify.com/sites/your-site-name/deploys`
- 构建设置: `https://app.netlify.com/sites/your-site-name/configuration/deploys`
- 环境变量: `https://app.netlify.com/sites/your-site-name/configuration/env`

