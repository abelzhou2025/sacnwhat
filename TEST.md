# 在 Cursor 中测试 OCR 功能

## 🚀 快速测试

### 方法 1: 使用测试脚本 (推荐)

1. **准备测试图片**
   - 将一张包含文字的图片放在项目根目录
   - 例如: `test.jpg`, `test.png` 等

2. **运行测试**
   ```bash
   npm run test:ocr ./test.jpg
   ```
   
   或者直接使用 tsx:
   ```bash
   npx tsx test-ocr.ts ./test.jpg
   ```

3. **使用 base64 字符串测试**
   ```bash
   npx tsx test-ocr.ts base64 <your_base64_string>
   ```

### 方法 2: 在 Cursor 中直接运行

1. **打开测试文件**
   - 打开 `test-ocr.ts` 文件

2. **设置断点** (可选)
   - 在代码行号左侧点击设置断点

3. **运行测试**
   - 按 `F5` 开始调试
   - 或者右键点击代码 → "Run Code"
   - 或者使用终端: `npm run test:ocr <图片路径>`

4. **使用 Tasks**
   - 按 `Cmd+Shift+P` (Mac) 或 `Ctrl+Shift+P` (Windows)
   - 输入 "Tasks: Run Task"
   - 选择 "Test OCR Function"
   - 输入图片路径

### 方法 3: 在浏览器中测试完整流程

1. **启动开发服务器**
   ```bash
   npm run dev:netlify
   ```

2. **打开浏览器**
   - 访问显示的 URL (通常是 http://localhost:8888)

3. **测试功能**
   - 上传图片
   - 查看提取的文本
   - 检查浏览器 Console (F12) 查看日志

## 📝 测试示例

### 测试单个图片

```bash
# 使用相对路径
npm run test:ocr ./test-image.jpg

# 使用绝对路径
npm run test:ocr /Users/abel/Desktop/test.png
```

### 测试 base64 字符串

```bash
npm run test:ocr base64 iVBORw0KGgoAAAANSUhEUgAA...
```

## 🔍 调试技巧

### 查看详细日志

测试脚本会自动显示:
- ✅ API 密钥是否存在
- ✅ 图片信息 (MIME 类型, 大小)
- ✅ API 调用状态
- ✅ 提取的文本
- ✅ 处理耗时

### 在代码中设置断点

1. 打开 `test-ocr.ts`
2. 点击行号左侧设置断点
3. 按 `F5` 开始调试
4. 代码会在断点处暂停

### 检查环境变量

确保 `.env` 文件存在且包含:
```
DEEPSEEK_API_KEY=your_key_here
```

或者:
```
GEMINI_API_KEY=your_key_here
```

## 🐛 常见问题

### 问题: "API key not configured"

**解决**:
1. 创建 `.env` 文件
2. 添加 `DEEPSEEK_API_KEY` 或 `GEMINI_API_KEY`
3. 重新运行测试

### 问题: "Cannot find module 'tsx'"

**解决**:
```bash
npm install
```

### 问题: 图片路径错误

**解决**:
- 使用相对路径: `./test.jpg`
- 或绝对路径: `/Users/abel/Desktop/test.jpg`
- 确保图片文件存在

## 📚 更多信息

- 调试指南: 查看 `DEBUG.md`
- 快速开始: 查看 `QUICKSTART.md`
- 部署说明: 查看 `README.md`

