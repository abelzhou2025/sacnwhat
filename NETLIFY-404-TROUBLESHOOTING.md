# 🔧 Netlify 404 错误排查指南

## 已实施的修复

### 1. 创建了 `public/_redirects` 文件
这是 Netlify 推荐的方式，文件会被自动复制到 `dist` 目录。

### 2. 更新了 `netlify.toml`
添加了 `force = true` 确保重定向规则生效。

### 3. 修复了 `index.html`
移除了不存在的资源引用（`/vite.svg`, `/index.css`）。

## 验证步骤

### 步骤 1: 检查构建输出

在 Netlify Dashboard 中：
1. 进入你的站点
2. 点击 "Deploys"
3. 查看最新的部署日志
4. 确认：
   - ✅ 构建成功
   - ✅ `dist` 目录包含 `index.html`
   - ✅ `dist` 目录包含 `_redirects` 文件

### 步骤 2: 检查文件结构

部署后，`dist` 目录应该包含：
```
dist/
├── index.html
├── _redirects          ← 这个文件很重要！
├── assets/
│   ├── index-[hash].js
│   └── index-[hash].css
└── ...
```

### 步骤 3: 测试重定向

1. 访问你的网站根目录
2. 访问任意路径（如 `/test`）
3. 应该都显示应用，而不是 404

## 如果仍然 404

### 检查清单

- [ ] 代码已推送到 GitHub
- [ ] Netlify 已重新部署
- [ ] 构建日志显示成功
- [ ] `dist` 目录包含 `index.html`
- [ ] `dist` 目录包含 `_redirects` 文件
- [ ] 环境变量已设置（如果需要）

### 手动检查

1. **在 Netlify Dashboard 中**：
   - Site settings → Build & deploy
   - 确认 "Publish directory" 是 `dist`
   - 确认 "Build command" 是 `npm run build`

2. **检查构建日志**：
   - 查看是否有错误
   - 确认 Node.js 版本是 18+

3. **检查 Functions**：
   - Site settings → Functions
   - 确认 Functions 目录是 `netlify/functions`

### 强制重新部署

如果修复后仍然有问题：

1. 在 Netlify Dashboard 中
2. Deploys → Trigger deploy → Clear cache and deploy site

## 常见问题

### Q: 为什么需要 `_redirects` 文件？

A: `_redirects` 文件是 Netlify 的原生方式，更可靠。它会被复制到 `dist` 目录，确保重定向规则生效。

### Q: `netlify.toml` 和 `_redirects` 哪个优先？

A: `_redirects` 文件优先。但两者都配置可以确保兼容性。

### Q: 如何验证重定向是否工作？

A: 
1. 访问你的网站
2. 打开浏览器开发者工具
3. 查看 Network 标签
4. 刷新页面
5. 应该看到请求返回 200，而不是 404

## 下一步

1. ✅ 提交并推送代码
2. ⏳ 等待 Netlify 重新部署
3. ✅ 验证网站可以访问
4. ✅ 测试所有功能

